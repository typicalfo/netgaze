# NetGaze Fix Plan\n\n**Date:** Mon Nov 24 2025\n\n## High Priority (Blockers)\n1. **CLI Dead Code** (`cmd/root.go:109-194`): Remove duplicate output functions\n2. **Orchestrator** (`internal/collector/collector.go`): Uncomment traceroute/whois/asn/geo\n3. **TUI Collection** (`internal/ui/model.go`): Add `collectCmd` in `Init()` to trigger `collector.Collect`\n\n## TUI Improvements (\"looks terrible\")\n1. **Layout** (`internal/ui/layout.go`): Fix responsive widths/heights, use full terminal\n2. **Styling** (`internal/ui/styles.go`): Increase contrast, bolder sections, fix padding/margins\n3. **Progress** (`internal/ui/model.go`): Real-time collector status spinner/table\n4. **Content** (`internal/ui/layout.go`): Better key-value alignment, truncate long values\n5. **Tabs** : Ensure Ask tab behavior and spacing are correct\n\n## Medium Priority\n1. **Vet Fixes**: slices.Contains, IPv6 dial, unused params\n2. **Tests**: Add cmd/ui coverage (>80%)\n3. **Perf**: Benchmark full run <12s\n\n## Steps\n1. Edit files per above\n2. `go fmt ./... && go vet ./...`\n3. `go test ./... -cover`\n4. `go build ./cmd`\n5. Test TUI: `./netgaze google.com`\n6. Commit: \"fix: resolve code review issues + TUI overhaul\"\n\n**Est Time:** 2-3 hours